# Python 3.11+ (Django 5.2 supports 3.10 - 3.13)
Django==5.2.17
django-filter==26.1

# Reads .env; settings.py imports it at module level, so the app will not
# start without it.
python-dotenv==1.2.2

# PDF generation (student lists, attendance reports)
reportlab==5.0.0

# SMS / WhatsApp notifications
twilio==9.10.9

# Image uploads (profile_pic)
Pillow==12.3.0

# Static file serving + DB config for deployment
whitenoise==6.12.0
dj-database-url==3.1.2
gunicorn==26.0.0
psycopg[binary]==3.3.4

# FCM push notifications
requests==2.34.2
